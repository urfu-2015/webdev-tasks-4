{
  "name": "webdev-tasks-4",
  "scripts": {
    "cc": "stylus index.styl --out ./compiled --compress",
    "c": "stylus index.styl --out ./compiled"
  },
  "dependencies": {
    "npm-run-all": "^1.6.0",
    "stylus": "^0.54.2"
  }
}
